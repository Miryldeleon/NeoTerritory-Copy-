# Changelog

All notable changes to NeoTerritory are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/) loosely, but releases use a
hybrid label of SemVer + ISO date (e.g. `0.1.0 — 2026-05-12`). This file is
generated from Conventional Commit subjects by `scripts/generate-changelog.mjs`.

## 0.1.0 — 2026-05-12

### Added

- **gdb-runner**: stateless re-run + drop trophy E2E explainer from studio (04a777d)
- **about+screencap**: role delegation copy + dynamic-aware pattern-card capture (22e4318)
- **entry+docs**: static tester picker, /choose required, docs sidebar, alpha-only trophy framing (d18b403)
- **tests**: static-analysis phase via cppcheck; integration + e2e removed with reasoning (30113aa)
- **docs**: drop AI-pending placeholders, use beginner fallbacks (0400b86)
- **deploy**: record deployed SHA on AWS + add diff-since-deploy dispatcher (177f37d)
- **backend**: NEOTERRITORY_BIN_WSL=1 spawns microservice via wsl.exe (167c7f9)
- **tour**: capture all 8 studio screenshots locally + bypass tile-picker login in capture script (7560dde)
- **research**: bento + click-to-popup tiles; design-rationale section with 2020-2026 citations (f2b98eb)
- **studio+docs+mechanics+joyride**: docs pending banner gone, Stage 4 JSON-parser visualization, session-aware Joyride, Method semantic-from-structure note (9a0c7c8)
- **patterns**: per-pattern sources[], Why-GoF block, /research evidence section; fix Testing button silent no-op (2536a2e)
- **marketing**: patterns citation header + tokens popover; chooser studio sub-choice (tester vs account) (6d7d3b8)
- Trophy-aware studio, /research+/about split, AVP-ready home, Playwright all-samples spec + GHA E2E gate (21a1c27)
- **marketing**: unify /patterns (drop All/GoF), inline SVG diagrams for home+mechanics, Playwright marketing-capture script (1564269)
- **marketing+studio**: refinement turn — tab-aware Joyride, Try-it chooser, /learn into /patterns, 7 more patterns from Nesteruk 2022, simpler topbar (876f8e0)
- **marketing+studio**: expansion turn — Nesteruk 2022 reference + thesis bibliography, full-coverage non-sticky nav, sample picker, in-studio Joyride tour, Playwright capture (e38866b)
- **marketing**: Sprints 0-12 — features-first home, audience-first IA, new surfaces (/why /mechanics /patterns /tour /research), thesis-rewritten /about (b623496)
- **analyze**: per-file 1000-token cap with live counter and server gate (e27a11a)
- **supabase**: mirror users to cloud + GitHub-secret sync helper (1ff9daf)
- **auth**: Google sign-in for Developer + Student via Supabase, Tester keeps seat (1abc49f)
- tools/dev-setup folder + EntryChoice hides Tester card when admin toggle is off (5255649)
- tools/dev-setup for other devs + tester-toggle hides Tester card on EntryChoice (3cb9289)
- **setup**: install gcloud CLI + automate Google OAuth provisioning (5f1d1d5)
- admin tester-toggle + automated local Supabase CLI + AWS Supabase removal (d8cde4d)
- survey-gated run persistence + static-only doc banner + solid title + AI probe (361f556)
- **ui+ops**: drop confidence chrome, add Supabase auth scaffolding, sync guard, studio-aware merge workflow (0c2e6ba)
- **test-runner**: stream per-phase verdicts via shared idempotent runId (fa1eb46)
- **deploy**: add --clean flag to deploy-aws.sh for from-scratch AWS rebuild (38f1750)
- **frontend**: Learn tab is overview-only with referral; Module 1 covers ambiguity + connotative-definition rule (c99655e)
- **microservice**: negative_signature_categories gate; eliminates Adapter false positives (4198aa1)
- **microservice**: combo-based connotation dictionary, no naming-convention basis (47fed4f)
- **microservice**: connotation-rule candidate filter — strict AND of lexeme category and grammar (8b6ff8e)
- **start**: port kill switch — detect and free held :3001/:5173 before binding (2d19932)
- **ui**: rename studio tabs, add icon set, polish entry-choice (3b1e84e)
- **catalog**: D37 step 1 — per-pattern fallback_doc templates (aec7916)
- **infra**: default Docker test-runner pods + firejail fallback ON (05a7c3d)
- **scripts**: unified flag-based scripts/rebuild.sh + start.sh rebuild (9394984)
- **admin**: compound log filtering — user, type, dates, online, activity (5ae6df4)
- **admin**: add TN to F1 metrics; cascade per-run survey with run (831783d)
- add learner and developer entry paths (509ff28)
- apply devcon-inspired UI theme (1571916)
- **tagging**: multi-pattern propagation with bidirectional hierarchy spread (4a94d4b)
- **docs**: add Documentation tab with MD/PDF/DOCX export (7f519fc)
- **pod**: tighten per-user pod and file upload limits (fd7a517)
- **deploy**: add smoke test + --restart-only recovery flag (0dd3a44)
- **deploy**: add visible progress + heartbeat so deploy never looks stuck (eb50163)
- **frontend**: single class-rooted tree with mainDesignPattern + review status (5e6b685)
- **learning**: add 'Before you start' prerequisite checklist to each lesson (55ff199)
- **deploy**: forward ENABLE_TEST_RUNNER + TEST_RUNNER_SANDBOX so per-user pods serve /api/runs in prod (f83c9c1)
- **hooks**: regenerate docs/auto on every commit when source changes (8613ed5)
- **docs**: auto-generate ops/scripts reference from annotated source (4222005)
- **learn**: per-pattern top-level routes (36acba7)
- **learn**: split into per-family pages with lesson sidebar and studio prefill (a44dcb7)
- **learn**: add pattern-catalog curriculum modules (accb067)
- **backend**: swap bcrypt->bcryptjs, add optional HTTPS listener, add axios (b5a2f01)
- **scripts**: add --aws/--both/--local flags and remote bootstrapping (820a322)
- integrate AWS deployment and auto-installation; switch to Fastify; refactor key management (b3732c6)
- **deploy**: production-mode hardening — Lightsail port-open + CORS + reachability probe (abcb4da)
- **supabase**: wide mirror — every admin-visible table syncs to Supabase (feffa07)
- **deploy**: pre-flight supabase connectivity probe (f2e0570)
- **ai+deploy**: Gemini provider + auto-chmod + Supabase key-shape guard (0d715a9)
- **deploy**: lightsail-launch.sh — first-boot provisioning for AWS Lightsail (21c396d)
- **deploy**: add --source ship mode (remote builds image from tarball) (c00617e)
- **theme,motion**: light-mode-aware effects + spring transitions with blur (3fe25c9)
- **deploy**: one-shot AWS deploy script + Supabase admin-log mirror (3984c62)
- **ui**: viewport overflow guard + ReactBits-style polish on studio + admin (0cceaa3)
- **annotated**: recursive cascade + transitive per-class revert (7d7cc3d)
- **learn**: static Pattern Atlas — tag, source, generated test, explanation per pattern (51f34ed)
- **about**: real Andrew + Miryl entries with skills + image gallery, fed by scraper distiller (a289e09)
- **annotated**: tagged-class masterlist + per-class revert affordance (199352e)
- **scraper**: hydrate lazy images + collect from <img>, srcset, SVG <image>, video poster, CSS background-image (e6eaa1f)
- **scraper**: auto-scroll the picked container until lazy-load is exhausted (84e637b)
- **start**: add prod mode that runs npm run build + node dist + vite preview (34c4e5f)
- **annotated**: add Not-this-pattern affordance on inheritance-driven parents (0f5fe30)
- **scraper**: land deferred D35 manual-login scraper behind admin + env flag (5973505)
- **frontend**: add public marketing surface (hero, learn, about) + path router (29e1b78)
- **annotated**: drive subclass cascade from inheritance_driven_patterns.json (fa1c4af)
- **annotated**: centralised model, subclass cascade, Review consolidation (8a53e36)
- **annotated**: split decided panel; gate accuracy on user disambiguation (4bf5b96)
- **annotated**: strict per-line colors, chrome greying, popover dedup (f5c3d37)
- **annotated**: full-catalog rival picker on every class decl (2ffd066)
- **admin-survey**: three dedicated tables + multi-sheet XLSX export (6442c3e)
- **admin-survey**: full Questionnaire B labels + per-question distribution + CSV export (24a4580)
- **heartbeat**: consolidate microservice + docker + AI + per-user pod polling onto heartbeat (47d8f50)
- **workflow**: sequential tab gating + locked validation tab until gdb passes (a8f65db)
- **heartbeat+review**: piggy-back health on heartbeat, single save+submit button (5d90a3c)
- **payload+stdin+timeout+cta**: payload validator folder, program-input stdin, 60s run cap, single submit-and-save button, family-aware pattern freq (21bfadf)
- **pods**: diagnose docker disabled state — env vs PATH vs daemon (d040e10)
- **pods**: auto-build cpp-pod image on boot, default ON, expose Docker status in status card (8d30dd4)
- **gdb+pods**: split console output panes, per-tester Docker pod isolation (837f172)
- **annotated+stats**: tighten ambiguity to microservice-tagged, add pies, 7-day window, drop nav center (84664d3)
- **stats+gdb+annotated**: test-run accuracy, session-bound GDB, sticky nav, distribution rename (5758a69)
- **annotated+admin+auth**: tag propagation, side-by-side wrap, tokens regression, 5-min admin poll, seat reclaim (6eb2874)
- **runner+admin+nav**: nav scope-aware, GDB faster, admin unthrottled, log-purge cascade, runs in Logs tab, per-user moved to Users (c99159b)
- **runner+admin**: write each user file to runDir, add audit log + run delete, exclude admins from totals (bc84dc6)
- **logs+ui**: persistent slot retention, class-nav cross-tab, modal centering, frontend log tab (b63de07)
- **test-runner**: full-source bundle + introspect.hpp middleman, flexible templates (2ef3136)
- two-phase GDB runner (compile then test), tagged-vs-missing class pills (757a203)
- brace-matched class scope, GDB Chrome-tabs + ephemeral runs + rate limit, file cap env (96d74f5)
- **tier-2**: multi-file submission, GDB Runner tab, sealed envelope, deploy doc (8c3c3e2)
- hoist scoring banner, last-tab Submit, JWT autogen, bootstrap.ps1 (41f727c)
- **auth**: revoke JWT on sign-out, disable claimed seats, defer queue/EC2 (de08c19)
- **scoring**: replace arbitrary constants with Wilson 95% CI lower bound (b894038)
- line-coverage probability score, review sub-tabs, single run-prompt (4b29f80)
- scope-locked retag, scoring explainer, review-tab gating, gate URLs (4d807c4)
- light/dark theme toggle, wider seat picker, F1 table, admin gate skip (6fb8a83)
- tab-bound heartbeat, admin presence filter, restore React HTML mounts (4e87057)
- per-class color propagation, drop auto-modal, beginner pattern cards (553b0b2)
- impl-fit templates, per-class retag picker, selective seat reset (6c21116)
- input maxLength constraints and STRIDE security doc (6a463da)
- collapsible pattern cards, wider tester login, user search (20b01c8)
- scope propagation on resolve + class validation checklist (75a13d6)
- global scope coloring + admin tabs (Runs, Complexity, F1 metrics) (e35d7a1)
- enrich pattern catalog with C++ lexemes and false-positive guards (a5f818f)
- per-line grey gradient for contested pattern overlap (f925861)
- tabbed studio, async AI commentary, survey pipeline (0c0081f)
- grey-on-ambiguous, vivid-on-confident annotation color policy (cbf5267)
- line-click popover with pattern colors and ambiguity surface (ff1180e)
- tester seat claim, scope-based annotations, catalog lexemes (0d8da42)

### Fixed

- **why+theme**: flip text tokens in light mode + add 2020-2026 cited evidence section (a887882)
- **studio**: restore #status-title as sr-only aria-live region (840bc5e)
- **test-runner**: make cppcheck phase work + drop alpha-testing copy from runner UI (6beb120)
- **studio**: remove StartHereRail; joyride auto-fires once per session, never per-tab (4a922a6)
- **thesis**: remove duplicate xmlns:wp on w:document root (04102c3)
- **run-tests**: surface 'no patterns' + dispatch failures instead of silent SSE close (9d7745f)
- **home**: bento grid always packs into a rectangle (no missing cells) (1912a0d)
- **e2e**: widen Strategy class regex; tolerate samples with no class tree (4b62932)
- **e2e**: single shared seat + resolve ambiguity by picking first candidate (51e746e)
- **e2e**: bypass picker, fill editor from disk + release seat in afterEach + bump MAX_TEST_USERS (c6d8291)
- **e2e**: pre-dismiss StartHereRail + Joyride tours in spec init script (6ca6ffd)
- **ci**: set ENABLE_TEST_RUNNER=1 — NODE_ENV=production blocks the compiler-based runner by default (0fb0b79)
- **e2e**: bypass tester UI via /auth/claim + skip surveys via nt-entry-flow=developer (28bb9a3)
- **ci**: seed Devcon test users in Playwright workflow + add /mechanics Trophy-vs-data-structure section (3f9c3be)
- **auth**: GoogleCallback stuck on "Signed in" — race between replaceState + navigate (853f68c)
- **auth**: match users table schema (password_hash, NOT NULL email) (7a39874)
- **test-runner**: include className in dedup key + atomic single-batch finalize (67cfaff)
- **test-runner**: fall back to local sandbox when pod compile fails opaquely (02f0cdd)
- gdb runner ci — use dedicated singleton class and relax classText filter (24bd9fa)
- disable pod wiring in annotated-source CI path (10728ed)
- gate pod warmup for CI claim and analyze flows (339bb27)
- **cmake**: include runtime sources for syntactic entrypoint (4c47d53)
- **ci**: include dev dependencies in production no-leak job (3b78ae9)
- **cmake**: include Modules/Source headers for transform internals (850ccc8)
- **ci**: align linux include path and install dev deps in prod-check job (d70b675)
- **docker**: pass Backend/.env to runtime container so JWT_SECRET persists (b0154ff)
- **test-runner**: unit-test driver compile-error fixes (47f605f)
- **microservice**: skip *.fallback_doc.json sidecars in pattern catalog loader (6f19815)
- **start**: force run_rebuild_list to return 0 so set -e doesn't kill the run (19cb9a8)
- **start**: resolve microservice BIN_PATH on WSL and multi-config builds (899959e)
- **rebuild**: resolve microservice binary path on WSL and multi-config builds (ea05b28)
- **annotated**: stop pattern cross-pollination across classes (55c297a)
- **reviews**: resolve questions.xml from source when dist lacks asset (c2e1f83)
- **admin**: persist tabs across switches; seed tester seats; rate-limit refreshes (3edd6ee)
- **dispatcher**: suppress false-positive pattern emission via negative signals (115aae0)
- **backend**: remove --poll flag dropped by tsx 4.21 (f551f4e)
- make microservice cmake macos aware (2ed9f3d)
- **catalog**: probe both root depths for pattern_catalog in dev and compiled modes (c6a3ac2)
- **microservice**: add build-wsl candidates and fix root resolution depth (5bfa413)
- **templates**: replace nt::touch(void_expr()) with (void)(expr()) across all pattern test templates (d7936df)
- **validator**: raise token limit to 500; add live counter in frontend (06a5fcb)
- **pod**: raise default memory limit from 48m to 256m (72f484b)
- **ranking**: compare signal weights, not raw hit counts, in per-line win condition (0ebf6f5)
- GDB template path in dist mode, class tree review-first sort, handlePickClass stale closure (eccdd21)
- **validator**: raise token limit to 500; add live counter in frontend (d825f0c)
- **deploy**: white-page root cause + RAM-aware build + tolerant smoke test (8344443)
- **deploy**: revert parallel node builds (1GB Lightsail OOM) + raise SSH keepalive (a6e9739)
- **podManager**: unblock per-user pod compile by tar-streaming files + adding exec to /work tmpfs (6e54779)
- **frontend**: make GDB runner session-based across tab switches (595ad5c)
- **deploy**: clean root-owned dist+node_modules and quote sandbox env value (386c7c1)
- **api**: correct /api/sample paths under compiled dist + enable docker pods in deploy env (37d08eb)
- **repo**: force LF for *.sh to prevent WSL bash CRLF errors (bbf7321)
- **server**: drop upgrade-insecure-requests + HSTS until SSL is wired (24d9d8b)
- **server**: serve Frontend/dist not Frontend/ source in production (eb8f4e0)
- **deploy**: bind 0.0.0.0, ship full env, fix dotenv path + microservice resolution (a36f873)
- **scripts**: rename-then-delete fallback for cross-platform node_modules (2033082)
- **scripts**: detect cross-platform node_modules and reinstall (cbbae65)
- **about**: align principles with CLAUDE.md, replace mismatched team photos (2d3c58a)
- **build**: env-tagged CMake build dirs + scrub absolute paths (d7fd65d)
- **scripts**: add UTF-8 BOM to PS1 files and quote $i in deploy probe (b582f64)
- **frontend**: regenerate lockfile to restore rollup optional dependency (7e50c98)
- default production backend bind host to 0.0.0.0 (2dd24ce)
- serve frontend from correct production path (ea2e212)
- **deploy**: auto-copy SSH key off /mnt/c when WSL+NTFS blocks chmod 600 (16fc6d0)
- **docker**: also COPY Microservice/pattern_catalog into frontend-builder (bccdf40)
- **docker**: mirror repo layout in frontend-builder so cross-dir ?raw imports resolve (3654038)
- **docker**: include devDeps for backend build, then prune (265d6dc)
- **deploy**: switch source-ship to allowlist — only build-essential paths (ec6e539)
- **annotated**: strip-aware rebuild of ambiguousLines + inScopePatterns (ba3b9e4)
- **verify**: retry docker daemon up to 30s instead of giving up immediately (6f894de)
- **annotated**: per-(className, canonical-pattern) strip on activePatterns + allAnnotations (b4c28b4)
- **deploy**: make Lightsail deploy actually work (frontend build, native deps, k8s exclusion) (f638f57)
- **studio,admin**: pin .src-popover (and other fixed descendants) to viewport again (b5c5011)
- **scraper**: pin overlay to viewport top-right even when host page transforms <body> (64ff036)
- **annotated**: strip propagated descendant tags by canonical name (ff01fa4)
- **scraper**: extract per immediate child of picked container, not flattened innerText (27d5689)
- **annotated**: cascade counts only matcher-tagged patterns on the child (c333ee6)
- **annotated**: strip microservice annotations for dropped subclass classes (1a56a9a)
- **annotated**: subclass cascade locks children to parent's pattern; rebuild Review card (115f635)
- **annotated**: align greying with picker; CamelCase canonicalisation; click-to-undo (0baf72a)
- **annotated**: exclude Review sentinel from ambiguity counts and rival chips (bad05ea)
- **annotated**: trigger rival picker on scope-union ambiguity (3e4f254)
- **backend**: cache imageExists so /api/health returns under WSL2 (6bbc271)
- **review**: route per-run Likert through run_feedback, not manual-review (709291a)
- **review+backend**: one-click save+submit really fires both, lock + 3s redirect, idempotent submissions (5f52b26)
- **admin+db**: full activity-table cascade + ON DELETE CASCADE migration on FKs (889f424)
- **admin**: cascade manual_pattern_decisions before deleting analysis_runs (a42e77f)
- **review**: single constant-label button — "Save and submit for validation" (ecca1a2)
- **validation**: dedup tagged classes across patternKey formats, drop per-row save buttons (d5cbc7e)
- **validation+admin**: drop quick-rating popup, hide saved actions, dedupe tagged rows by resolved tag, add idiom family to pies (ac4d918)
- **start.sh**: force linux TMPDIR so tsx ipc pipe works under WSL (ab5918c)
- **annotated+pods**: drop AI loading gate, make every Docker call non-blocking (85f9f4a)
- **annotated**: wait for microservice tags before rendering, document docker disabled path (6d4ecc2)
- **annotated+gdb**: override colour authoritative, CTA auto-runs, skipped tests no longer count as fails (06b442c)
- **annotated**: class ambiguity uses the SAME signal as the popover badge (0dbd02f)
- **annotated**: two-step ambiguity — class must be tagged first, then in-scope competitor flips it (9204f25)
- **annotated**: nav + missing pill STRICT to microservice-tagged classes with multi-pattern conflict (7d07299)
- **annotated+stats+gdb**: in-scope counts as tagged, family pie restricted to 3, pie math, auto-expand errors (b72d0b8)
- **annotated+stats**: annotation-driven ambiguity, missing==ambiguous, viewport-corner nav, chip tooltips, sorted distribution (55d2f1f)
- **annotated**: pull in-scope-ambiguous classes into population, restructure shell, gate review on GDB pass (26c6817)
- **introspect+annotated**: kill nt::nt double namespace, ambiguity-aware tag pill, side-by-side cards (0a58626)
- **runner+gdb-tab**: templates compile cleanly, criteria emit, tree UI, ambiguity gate, full logging (e437745)
- **auth**: stop revoking JWT on pagehide beacon (5573208)
- **frontend**: style survey modals, silence admin pre-auth 401 race (aea84bc)
- probe singleton accessor, ambiguous-only class nav, multi-file save/load (2666e61)
- bottom-right class-nav overlay, singleton template uses std::is_copy_constructible (c6305d9)
- **admin**: drop token-error banner, move online pill to topbar (f1db377)
- autoconfig test runner, write tag-progress on resolve, trim heuristic usage (351c422)
- confine retag color to class scope, Chrome-tab file picker in Submit (005e3f6)
- orphan Review tags, retag chip clarity, WCAG contrast helper (6c363f4)
- surface microservice patterns despite SIGSEGV exit code (d9c3c13)
- drop duplicate .src-line rule shadowing per-pattern colors (ca7ed2f)

### Changed

- **studio**: flatten layout to one natural page scroll + slim topbar (2a51db4)
- **site**: rename /research → /docs, sidebar layout, JSX extracted into sections/ (e978e4f)
- **marketing+studio**: Sprint -1a/-1b triage — kill motion overload, open StudentLearningHub, add Start Here studio rail (00c4e40)
- **rebuild**: split into per-component scripts, opt-in rebuilds in start.sh (2a912bf)
- **frontend**: rename lib→logic; sync sidebar/popup ambiguity; add rebuild scripts (458d392)
- **cascade**: decouple subclass tagging; nuanced cases a-f + undo (e0e01ed)
- **dispatcher**: revert negative-signal suppression; add observability (35b2228)
- **deploy**: make remote build incremental + parallel to cut deploy time (eeeab50)
- **ops**: regroup shell modules under ops/{bash,powershell}/ (ec78116)
- **start.ps1**: decouple into scripts/start/{lib,commands}/ modules (541d9ce)
- **start.sh**: decouple into scripts/start/{lib,commands}/ modules (fc2fe20)
- **annotated**: drop Revert chip, transitive undo on existing button (15e8ab4)
- **backend**: masterlist + docker watcher for non-blocking /api/health (7dfcc5d)
- pin source popover top-right, drop the redundant retag system (eff692c)
- replace centered retag modal with upper-right inspector (517e953)
- migrate frontend+backend to TypeScript and harden security (4481ce1)

### Docs

- **thesis**: embed Activity/UseCase/Class diagrams above their captions (b291241)
- **thesis**: trophy refs, defense PPT, promote Diagrams to its own H2 (2e994a3)
- **testing**: split user-code testing from product-self testing across surfaces (9cc2e00)
- **thesis**: split Ch5/Ch6, add Use Case diagram (PUML+MMD+PNG+SVG) (8ef1d74)
- **thesis**: refresh algorithm + use cases, expand Ch2, add Ch5, fill Ch4 (3ad94d8)
- define CI and production pod warmup separation (c2f4cc0)
- lock dev-only step diagnostics and CI no-leak blueprint (eab6c6e)
- **design**: D37 — AI auto-documentation pipeline contract (b64151b)
- **claude**: rebuild default is always full; AI never auto-adds --skip-* (d9a7220)
- **rules**: require git push after commit each prompt (bf4f4d7)
- **claude**: add rebuild decision matrix for AI-driven minimal rebuilds (0eef75f)
- remove homepage scope section (333bd17)
- make homepage beginner friendly (8db4ad7)
- simplify homepage for learners (b47681f)
- align homepage with thesis brief (24ba497)
- define local docker session host (cf208b0)
- **ops**: add docstrings for ensure_node_modules / build_microservice / write_dev_env (34aa74b)
- reinforce mandatory commit rule in AGENTS.md (8557759)
- **infra**: document docker pod migration plan + current disabled state (01239d3)

### Internal

- **gitignore**: cover the screenshots/ path the studio walkthrough actually writes to (e485501)
- **playwright**: codify algorithm-known limits + D63 audit baseline (d6f3b9b)
- **playwright**: resolve ambiguity before reading pattern badges (0ca3f5c)
- **playwright**: fix isVisible race + scope e2e gate to all-samples.spec.ts (26a2ff1)
- **deploy**: add post-deploy-smoke job for live AWS verification (80a68ac)
- **backend**: wire vitest, extract candidateFilter, assert cppcheck phase (8d83867)
- **playwright**: cover 15 samples with structural + regression contracts (6e1ad93)
- **playwright**: harden selectors with data-testid + crash-safe seat release (588f645)
- Revert "docs(testing): split user-code testing from product-self testing across surfaces" (2e97793)
- **e2e**: dynamic-aware studio screenshot pipeline (5189e7e)
- fe(studio): pin topbar with self-scroll fallback + hide lede on narrow widths (91950ae)
- fix+feat: real-account users skip tester gates + admin toggle for review survey (0d93a5f)
- **eval**: add accuracy probe + new-joiner code tour (9f3bb18)
- **deploy**: trigger redeploy with new Supabase + Google OAuth env (be6fb2d)
- fe(design): minor marketing polish + admin topbar/tab/section refresh (9846282)
- deploy(aws): propagate AUTH_PROVIDER + AUTH_SUPABASE_ANON_KEY + GOOGLE_OAUTH_* (c7836b1)
- **supabase**: trim local stack to auth-only + env-write helper (cc67784)
- **ui-merge**: three-way Miryl+Jo adoption with Miryl-priority + build gate (0d4bbfc)
- add merge-jo-ui workflow — selective UI adoption from Branch-ni-jo into main (b219198)
- fix template literals in gdb-runner diagnostic log (bash mangled backticks) (b2be29b)
- assert compile_run passed and log all phase verdicts in gdb-runner (f751043)
- remove pod activities from gdb-runner — CI runners too constrained for Docker pods (0a47abc)
- fix deploy --quiet error and add real Docker cpp-pod test (67a0649)
- disable pod Docker mode in gdb-runner job to prevent background image build blocking step (226fca0)
- cache playwright chromium browser binary across runs (90e5de5)
- pass microservice binary to gdb-runner job via artifact (1df29b4)
- backend: store enriched detected patterns in pending analysis (9d54723)
- fail gdb api flow when run-tests returns empty results (bc61681)
- disable pod warmup on claim in gdb runner flow (c59c20c)
- pin kind cluster name for image load step (e293c0e)
- seed tester accounts for API flow checks (8a789d7)
- CI: decouple pipeline into backend, frontend, playwright, pod, gdb, and output gates (6947e14)
- CI: build microservice first and add kind pod integration gate (3ee186a)
- **ci**: trigger provenance check after parser-fix (013f44f)
- Fix CI deploy job condition by moving secret checks to runtime (d7814a9)
- **ci**: trigger github actions smoke run (48d10da)
- **cd**: support rsa/private key secret variants for aws deploy (14005bb)
- **cd**: auto-deploy to aws after passing checks (ffdb4bc)
- add frontend/backend merge smoke checks with playwright (7535723)
- scope ui guard to teammate actors and detach from build jobs (2cadf16)
- enforce ui-only file scope for teammate pushes (1e947c7)
- enable github actions workflow (7131293)
- remove --poll in dev script package (e7a9afe)
- ui updates (c49f834)
- update current home page to branding of devcon (d63f758)
- ignore scheduled_tasks.lock and node_modules.stale dirs (1903812)
- remove accidentally committed lock and stale node_modules (8394ec0)
- point deploy target to new Lightsail instance 122.248.192.49 (8fbe4e3)
- capture current workspace state (c12f436)
- **gitignore**: block .pem/.key/ssh creds and scripts/.env.deploy (801b057)
- Revert "feat(annotated): add Not-this-pattern affordance on inheritance-driven parents" (a4c5379)
- Revert "feat(annotated): full-catalog rival picker on every class decl" (8a6c90d)
- Build backend in image; improve scripts & docker (37b0c98)
- Consolidate root scripts and update hosts (6682307)
- **admin**: move Survey responses under the Reviews tab (2142537)
- **scripts**: requirements verifier is STRICT and SEQUENTIAL by default (769b346)
- **scripts**: shared requirements verifier wired into all entry scripts (a1e148c)
- **scripts**: unify dev pipeline into start.ps1 / start.sh (4bb90e9)
- remove agent.jo.md (Jo-Branch leftover) (c69849d)
- ui: lock in local UI changes before pull + add agent.jo.md (4d9751f)
- Add last_active, admin log controls & UI improvements (173996c)
- add clean-browser scripts for fresh Chromium sessions (496f613)
- Add fonts, popover resolve actions and UI refresh (339ea7d)
- Add per-line pattern overrides and AI status UI (4e20935)
- Add pattern lexeme hints & UI state overrides (774a303)
- Add scope-aware source view and annotation popover (4e5415d)
- Refine drawer UI; disable run inspect & counts (a42b29e)
- Revert "Add alpha diagnostics and runtime Devcon seat keys" (4d18bb0)
- Add alpha diagnostics and runtime Devcon seat keys (1ad703d)
- Final (50b25bd)
- Replace analyzer with microservice + AI docs (7b402f8)
- Add microservice modules and backend analysis services (319682c)
- Add inert stub implementations and cleanups (097fed3)
- Add analysis modules, trees, hashing & tests (e46e328)
- Add header interfaces for parsing and hashing (e0b014d)
- Add Microservice headers, docs and CMake (f2495e3)
- Make docs/Codebase the source of truth (62e03d2)
- Update docs for live class-analysis flow (844ecc2)
- Add branch/contract evidence types; fix SQL quotes (8561eb2)
- Clean ignored artifacts and align frontend docs (2ebd803)
- Update SyntacticBrokenAST parsing and backend config (1212d41)
- Add Diffing subsystem core and docs (bcd931e)
- Reorganize and update Codebase documentation (bbff65c)
- Refactor SyntacticBrokenAST docs & core code (87a92cd)
- Use .cpp.md suffix in PatternMiddleman docs (ddc1c12)
- Move legacy samples and update creational modules (894582e)
- Move project files into Codebase/ directory (2a149be)
- Reorganize docs into docs/Codebase (f4f760c)
- Add generated Codebase docs and docgen tools (89d786b)
- Consolidate docs into docs/ and add compiled docs (1141895)
- Restructure ParseTree headers and rename sources (d8b7600)
- Validate file pairing and track scope collisions (46d580b)
- Add Node/Express backend for code transformation (fdc56b2)
- Add factory invocation tracing and FNV-1a hashes (f68cb29)
- Refactor creational transform evidence (45e5e4c)
- Add factory->base transform & pattern view (76fb0a4)
- Move creational transforms; add structural hooks (dac63ba)
- Add parse-tree hash-links and codegen internals (0c8d031)
- Add Docker-only mode and Minikube recovery (84222b0)
- Add frontend UI and restructure codebase (502629a)
- Refactor parse_tree_code_generator into .inl files (616bb63)
- Track transform decisions & singleton->builder support (072b6be)
- Detect object/fallthrough factory returns (dc4de39)
- Refactor code structure for improved readability and maintainability (a7f3219)
- Ignore NeoTerritory.slnx and Setup.vdproj (444bcc5)
- Add behavioural/creational pattern detectors and docs (9797217)
- Add session/runtime scripts and refactor AST APIs (c53f8cc)
- Remove suggested RRL statement from docs (b29b621)
- File-aware symbol hashes and shadow filtering (ebb5e47)
- Add Windows setup script and update k8s apply path (f4c1355)
- Add session-orchestration infra and deploy scripts (1f9da63)
- Add software requirements and renumber tables (d970154)
- Lower baseline storage and add GitHub references (d4204c2)
- Add system specifications for NeoTerritory (9dcb067)
- Output test artifacts to TestResults directory (13d5b01)
- Add shadow AST, source units & contextual hashes (fd41c25)
- Add line-hash tracing and lexical hooks (0ce15b7)
- Enhance class symbol tracking and reporting (b2fadb4)
- Add SyntacticBrokenAST pipeline and outputs (cd1d8e4)
- Add creational pattern detectors and token updates (0cbe0b2)
- Switch to ParseTree-based AST pipeline (99be776)
- Add project instructions and IDE config files (2c24177)
- Add CFG-based syntactic tree construction and function call detection (7e529b3)
- Refactor AST pipeline and modularize function parsing (5090f17)
- Refactor and modularize preprocessor stripping logic (036380c)
- Add preprocessor and namespace stripping to AST input (45b17fc)
- Co-authored-by: Kollin123 <Kollin123@users.noreply.github.com> (0e59c7d)
- Refactor CMakeLists.txt for compiler settings and remove outdated documentation files (7a9ce86)
- Refactor CMakeLists.txt for improved source handling (7a6dbdb)
- Restructure project directories and add CMake config (9140eab)
- Improve AST handling and lexer support for char literals (#10) (23459e9)
- Add initial SyntacticBrokenAST module and VSCode configs (78aabd1)
- Delete .github/workflows/blank.yml (6fac20d)
- Create auto-defaults workflow for new issues (d1923f5)
- Complete cancelled PR #1: Add project documentation with team info, timeline, and contribution guidelines (#2) (8fa8b3d)
- Initial plan (#1) (652f7ce)
- Initial commit (bcaad8e)
